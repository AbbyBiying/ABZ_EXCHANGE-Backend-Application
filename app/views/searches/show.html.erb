<h1>search results for <% params[:search] %></h1>

<%= render @comments %>

<% if @comments.present? %>
  <%= render @comments %>
<% else %>
  <p>no comments found.</p>
<% end %>

<% if @users.present? %>
  <%= render @users %>
<% else %>
  <p>no users found.</p>
<% end %>
