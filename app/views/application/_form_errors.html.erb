<ul>
<% if object.errors.any? %>
  <%= pluralize(object.errors.count, "error") %> prevented your object from being created:
  <% object.errors.full_messages.each do |error_message| %>
     <li><%= error_message %></li>
  <% end %>
<% end %>
</ul>
