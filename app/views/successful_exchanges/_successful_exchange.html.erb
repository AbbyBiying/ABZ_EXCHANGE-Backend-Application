<div class="successful-exchange">
  <%= link_to successful_exchange.exchange.listing do %>
    Listing name: <%= successful_exchange.exchange.listing.name %>,
      description: <%= successful_exchange.exchange.listing.description %>
    <p><%= image_tag successful_exchange.exchange.listing.url, width:"200" %></p>
  <% end %>

  <%= link_to successful_exchange.exchange.offer.user do %>
    Offer name: <%= successful_exchange.exchange.offer.name %>,
      description: <%= successful_exchange.exchange.offer.description %>
    <p><%= image_tag successful_exchange.exchange.offer.url, width:"200" %></p>
  <% end %>
</div>
<br>
