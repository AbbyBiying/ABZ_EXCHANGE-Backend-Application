<div class="listing-index">
  <h2>All the listings</h2>
  <% if signed_in? %>
    <h2 class= "make-list"><%= link_to "Make New Listing", new_listing_path %></h2>
  <% end %>
  <%= render @listings %>
</div>
